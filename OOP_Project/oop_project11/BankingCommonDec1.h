#pragma once
#include <iostream>
#include <cstring>
#include <cstdlib>

using namespace std;
const int len = 20;

enum {MAKE=1, DEPOSIT, WITHDRAW, INQUIRE, EXIT};
enum {A = 7, B = 4, C = 1};
enum {NORMAL = 1, CREDIT = 2};
